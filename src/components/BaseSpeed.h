/*
    Base speed for a unit. The systems will first reset the entity's speed to
    this before applying changes.
*/

#ifndef BASESPEED_H_INCLUDED
#define BASESPEED_H_INCLUDED

struct BaseSpeed
{
    float speed;
};

#endif
