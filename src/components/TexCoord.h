/*
    Texture Coordinate components.
    Only used by vertex buffers and not directly as a component. But could be!
*/

#ifndef TEXCOORD_H_INCLUDED
#define TEXCOORD_H_INCLUDED

struct TexCoord
{
    float u, v;
};

#endif
