/*
    Label to define an entity as a Critter.
    This is only used to check if there are still critters alive in the
    current wave so we can start the next one.
*/

#ifndef CRITTER_H_INCLUDED
#define CRITTER_H_INCLUDED

struct Critter {};

#endif
